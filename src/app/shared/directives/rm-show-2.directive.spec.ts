import { RmShow2Directive } from './rm-show-2.directive';

describe('RmShow2Directive', () => {
  it('should create an instance', () => {
    const directive = new RmShow2Directive();
    expect(directive).toBeTruthy();
  });
});
