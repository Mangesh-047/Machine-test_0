import { NavdropDirective } from './navdrop.directive';

describe('NavdropDirective', () => {
  it('should create an instance', () => {
    const directive = new NavdropDirective();
    expect(directive).toBeTruthy();
  });
});
