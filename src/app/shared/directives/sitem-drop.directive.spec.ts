import { SitemDropDirective } from './sitem-drop.directive';

describe('SitemDropDirective', () => {
  it('should create an instance', () => {
    const directive = new SitemDropDirective();
    expect(directive).toBeTruthy();
  });
});
