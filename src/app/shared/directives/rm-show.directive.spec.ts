import { RmShowDirective } from './rm-show.directive';

describe('RmShowDirective', () => {
  it('should create an instance', () => {
    const directive = new RmShowDirective();
    expect(directive).toBeTruthy();
  });
});
